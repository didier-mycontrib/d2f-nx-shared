<div>
    <label class="block text-sm">{{effectiveLabel()}}:</label>
    @if(formRef()){
          <select [formField]="formRef()[name()]" >
              @for(v of items(); track v){
                <option>{{v}}</option>
              }
          </select>
    }@else{
        <select [(ngModel)]="fieldData" name="name()" >
         @for(v of items(); track v){
                <option>{{v}}</option>
              }
          </select>
    }
</div>
